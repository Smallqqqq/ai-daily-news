---
tags: [ai, tools, practical, daily-picks]
date: 2026-08-26
title: AI实用工具：生成式 UI、IDE 扩展与 MCP 抓包三利器
summary: 三个工具覆盖生成式 UI、IDE 扩展与 MCP 调试：json-render 用组件目录护栏约束 AI 产出 UI；Antigravity 官方扩展进驻四大 IDE；mcpsnoop 抓取每次 MCP 工具调用。
author: smallQ
---

# AI 实用工具 · 2026-08-26

![[summary.png]]

  今天三个工具站在一条线上：AI 生成的界面、AI 住进去的编辑器、AI 伸出去的调用。json-render 管的是模型产出的 UI 怎么不越界，Antigravity 管的是 Google 的编码 Agent 怎么进你现有的 IDE，mcpsnoop 管的是 Agent 调外部工具时你终于看得见它发的是什么。

---

## json-render — Vercel Labs 的生成式 UI 框架，AI 产出受护栏约束的界面

![[json-render-demo.png]]

  AI 生成 UI 的老问题是信任：让模型直接吐 React 组件代码，它会发明不存在的组件名、塞进错误的 props，每次生成都像开盲盒。Vercel Labs 的 json-render 换了一条路——模型不写代码，只输出一份受 schema 约束的 JSON spec，渲染由预先注册好的组件目录完成。它解决的问题用官方一句话说就是：生成动态、个性化的 UI，同时不牺牲可靠性。

  工作原理分三层。第一层是 catalog（组件目录），你把允许使用的组件和动作注册进去，AI 只能从这个目录里选，没有注册的组件连名字都不会出现在它的输出里。第二层是 schema，JSON 输出必须匹配预定义结构，每次都是同一个形状。第三层是 SpecStream，模型边生成边流式渲染，用户看到界面逐块长出来，不用等整份 JSON 生成完。官方管这套设计叫 Guardrailed——护栏是给模型画的，不是给用户画的。

  渲染器的覆盖面是它和同类拉开差距的地方。同一份 JSON spec，可以渲染成 React、Vue、Svelte、Solid 网页组件，可以变成 React Native 移动端界面，也可以走 Next.js 变成带路由的完整应用。更偏门的目标它也有：Remotion 视频、React PDF 文档、React Email 邮件、Ink 终端 TUI、Satori 生成的 OG 图。附带 36 个预构建的 shadcn/ui 组件开箱即用，还配了 devtools、Redux/Zustand/Jotai 状态适配器和 MCP Apps 集成。实际使用场景两个：聊天产品里让模型根据用户提问动态生成界面（生成式 UI 的典型形态），或者内部工具里让 AI 按模板搭页面，产出物是 JSON 而不是代码，方便存库、版本化、人工审核。

  数据摆在这里：15,978 stars，npm 上 @json-render/core 周下载 129 万次，v0.20.0 十天前刚更新。2026 年 1 月创建，七个月做到这个体量，Vercel 出品和生成式 UI 这个方向的热度各占一半。

```bash
# React（带 36 个 shadcn/ui 预构建组件）
npm install @json-render/core @json-render/shadcn

# 或 Vue
npm install @json-render/core @json-render/vue

# 起 demo 先看效果：examples/chat 目录里 pnpm dev
```

### 同类工具对比

| 工具 | 安装方式 | 核心特点 | 价格 | 热度 |
|------|---------|---------|------|------|
| **json-render**（今日推荐）| `npm install @json-render/*` | 模型只产 JSON，组件目录定边界，20+ 渲染器 | Apache 2.0 | ⭐ 15.9K · 周下载 129 万 |
| Vercel AI SDK（RSC generative UI）| `npm install ai` | React Server Components 里流式 UI，绑定 React 生态 | 开源 | ⭐ 高 |
| Tambo | `npm install @tambo-ai/react` | React 组件 + 运行时集成，模型直接驱动组件树 | 开源免费 | ⭐ 1.8K |
| StreamUI（LangChain）| `pip install streamui` | 模型自选组件按顺序流式输出，Python 侧方案 | 开源 | 社区 |
| 货拉拉 Figma MCP | MCP 服务 | 设计稿八步清洗转代码，上游是 Figma 设计稿 | Apache 2.0 | 8/21 已推荐 |

  上周推过的货拉拉 Figma MCP 解决设计稿到代码的转码，json-render 解决模型直接产出界面的运行时问题，同一条链上的两个环节。和 Vercel 自家 AI SDK 的 generative UI 相比，json-render 的产物是纯 JSON，不绑 React，多渲染器一份 spec 到处跑；Tambo 让模型驱动既有组件树，json-render 让模型从目录里搭新界面，约束方式不同。

⭐ 15,978 · Apache 2.0 · v0.20.0（2026-08-16 更新）· [GitHub](https://github.com/vercel-labs/json-render) · [官网](https://json-render.dev) · [npm](https://www.npmjs.com/package/@json-render/core)

---

## Google Antigravity IDE 扩展 — 官方把编码 Agent 搬进你正在用的四个 IDE

![[antigravity-demo.png]]

  谷歌 6 月停了 Gemini Code Assist 和 Gemini CLI，替代品 Antigravity 之前只有两个入口：Antigravity IDE（VS Code 的 fork）和命令行 agy。用惯了 VS Code、IntelliJ 的人想用 Antigravity，要么换编辑器，要么回终端。8 月 20 日这个缺口被官方补上了：Antigravity 扩展同时上架 VS Code、Visual Studio、IntelliJ 系和 Zed 四个平台，发布者 google.com，搜索时认准这个发布者，社区里已经有同名山寨扩展。

  它干的事是把 Antigravity harness 的全套能力塞进现有 IDE：专属侧边栏对话、逐 hunk 的内联 diff 审查、交互式计划检查、多步工程任务的执行。架构上扩展本身是个壳，首次启动自动安装本地的 agy 后端服务，模型请求走 Antigravity 的免费档或订阅。一个实践者的安装记录（DEV 社区 GDE 文章，8 月 24 日）里有个细节：装完登录 Google 账号后，它自动发现并激活了作者之前写的 install-gemini-code-review-action skill，再经 GitHub MCP 完成提交和推送。现有 skills 和 MCP 配置直接继承，不用重配。

  企业侧的发布是同一周的事。Google Cloud 8 月 20 日的公告把 Antigravity 挂进 Gemini Enterprise 订阅，带管理员和花费控制。有个口径要注意：Antigravity IDE（那个 fork 版）官方明确不支持企业客户，企业用户走扩展或 CLI。Gemini Code Assist 的存量用户 6 月被切走，现在这条迁移路径算是补上了。

  发布刚六天，安装量还在爬坡。免费档要求 VS Code 1.90 以上、一个 Google 账号即可。和 8 月 21 日推过的 OpenCursor 同属 IDE 扩展领域，但一个是 Google 官方平台级产品，背后是 Antigravity 全套 agentic 能力，热度和生态不在一个量级。

```bash
# VS Code：扩展面板搜索 "Google Antigravity"，认准发布者 google.com
# 或访问 VS Code Marketplace 安装：https://marketplace.visualstudio.com/search?term=Google%20Antigravity
# 首次启动自动安装本地 agy 后端，登录 Google 账号即用
# IntelliJ / Visual Studio / Zed：各自插件市场搜索同名扩展
```

### 同类工具对比

| 工具 | 安装方式 | 核心特点 | 价格 | 热度 |
|------|---------|---------|------|------|
| **Antigravity 扩展**（今日推荐）| 四个 IDE 插件市场 | 官方 harness 全套能力，继承 skills 和 MCP 配置 | 免费档起步 | 🏪 官方 · 8/20 发布 |
| Gemini Code Assist | 已停服 | 2026 年 6 月 EOL，被 Antigravity 取代 | — | 已退役 |
| Antigravity IDE | Google 官网下载 | VS Code fork，能力全内置，企业不支持 | 免费档 | 官方 |
| Antigravity CLI（agy）| `curl antigravity.google/cli/install.sh` | 终端 TUI，前 Gemini CLI 用户的主去处 | 免费档 | 官方 |
| 社区版 antigravity-vscode | Marketplace | 非官方，驱动本地 agy，36K 安装 | 免费 | 非官方 |

  选官方扩展而不是 36K 安装的社区版，理由在维护和口径：社区版（lyadhgod）明说自己"未获 Google 认可"，出问题没人兜底；官方扩展由 google.com 发布，后端服务、账号体系、企业合规都是一条线。用 Antigravity IDE 的人不必换，但不想换编辑器的人，这是目前唯一官方路径。

🏪 VS Code Marketplace · 发布者 google.com · 2026-08-20 发布 · [官方文档](https://antigravity.google/docs/ide/extensions/vscode/) · [发布公告](https://antigravity.google/)

---

## mcpsnoop — MCP 的 Wireshark，坐在真实数据路径上看每次工具调用

![[mcpsnoop-demo.png]]

  MCP 调试有个结构性盲区。官方 MCP Inspector 用起来的问题是：它自己作为客户端连服务器，看得到工具列表，看不到你的真实客户端（Cursor、Claude Code、Codex）实际发了什么。模型没调某个工具、或者参数传错了、或者调用挂住了，Inspector 都照不出来，只能翻日志猜。mcpsnoop 的解法像 Wireshark：它是个透明代理，把服务器命令包起来，你的客户端和服务器之间每一帧 JSON-RPC 都实时打在终端上。

  用法直接得过分。零配置体验就一条 `mcpsnoop demo`，看它自带的演示会话长什么样。真实接入是把 MCP 配置里服务器的 command 换成 mcpsnoop 包装，启动命令后面跟原来的服务器命令，客户端配置其余部分不动。之后每次工具调用、参数、响应、错误都逐帧显示，配合 Go 写的 bubbletea TUI 界面实时滚动。谁在什么时候调了哪个工具、带了什么参数、拿到什么结果，数据路径上发生的每一件事都看得见。

  它还有个少见的 CI 形态。仓库同时发布了一个 GitHub Action（kerlenton/mcpsnoop@v0.21.0）：把捕获的会话文件交给它，每个发现作为 code scanning alert 提进 GitHub，按你配置的门控决定 job 成败。这意味着"这个 PR 的 agent 会话里出现了一次异常工具调用"可以变成一条流水线上的阻断项，而不是事后翻聊天记录。

```bash
# 零配置体验
mcpsnoop demo

# 安装（三选一）
brew install mcpsnoop
go install github.com/kerlenton/mcpsnoop/cmd/mcpsnoop@latest
# 或从 Releases 下载预编译二进制

# 真实接入：把 MCP 配置里 my-server 的 command 改成
# "mcpsnoop <原服务器启动命令>"
```

### 同类工具对比

| 工具 | 安装方式 | 核心特点 | 价格 | 热度 |
|------|---------|---------|------|------|
| **mcpsnoop**（今日推荐）| `brew install mcpsnoop` | 透明代理看真实数据流，CI 门控形态 | MIT | ⭐ 343 |
| 官方 MCP Inspector | `npx @modelcontextprotocol/inspector` | 官方调试面板，自建连接，看不到真实客户端流量 | 免费 | 官方 |
| Chrome DevTools MCP | npm | 浏览器调试用，管页面不管 MCP 协议层 | 开源 | ⭐ 3.8K |
| 各客户端日志 | 内置 | 事后翻日志，调用挂起和未发生的调用都看不到 | 免费 | — |

  这个场景下它几乎没有直接竞品。Inspector 是测试工具，它是抓包工具，两者互补：先用 Inspector 验证服务器本身通不通，再用 mcpsnoop 看真实会话里模型到底发了什么。343 stars 不高，但它的 GitHub Action 形态是同类里独一份，能把 agent 会话审计接进 CI 这件事目前没有别的工具在做。

⭐ 343 · MIT · v0.21.0 · [GitHub](https://github.com/kerlenton/mcpsnoop) · [Kitploit 报道](https://kitploit.com/en/posts/github-kerlenton-mcpsnoop-v0190)
