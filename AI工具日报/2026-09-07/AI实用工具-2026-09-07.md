---
tags: [ai, tools, practical, daily-picks]
date: 2026-09-07
title: AI实用工具：InsForge 后端即 MCP、Magnitude 推理自动选型
summary: 一条给 Agent 配全套后端，一条把本地模型跑成 Agent 可直接用的推理服务，一条让研发把专利发明人一栏写上名字。
author: smallQ
---

# AI 实用工具 · 2026-09-07

![[summary.png]]

  今天三条各有角度：一条把后端基础设施整个变成 MCP 工具集，一条让本地模型不用再凭感觉选配，一条是研发绕不开的专利刚需。前两条工程向，第三条增值向。

---

## InsForge — 12.9K stars，把后端变成 MCP 的 agent-native 平台

![[insforge-demo.png]]

  编码 Agent 现在能写代码、跑测试、开 PR，一碰后端就卡壳：数据库表要人建、存储桶要人配、函数要人部署。InsForge 把这一步直接收进 Agent 手里——它是一整套 agent-native 后端（Postgres、认证、S3 存储、边缘函数、模型网关、实时订阅、Stripe 支付、托管），全部通过一个 MCP server 和 CLI 暴露给 Agent 调用，编码 Agent 自称后端工程师的短板就这样被填上了。

  原理上像 Supabase 换了个视角：把给人用的控制台，换成给 Agent 用的 API。CLI 创建项目时（`npx @insforge/cli create`）会自动给支持的编码代理装上官方 agent skills。MCP 的 fetch-docs、list-tables、list-functions 这类工具把后端真实状态（schema、函数、存储桶、日志）喂给 Agent，决策基于事实而不是猜。官方还做了数据库分支和可逆写入，Agent 改表可以安全试错。

  典型场景：跟 Agent 说"给我起一个带登录和文件上传的 app"，它自己建 auth、建表、开存储桶，你连控制台都不用打开。数据侧：12.9K stars、1.2K forks，YC 2026 春季班（$4M pre-seed），上线当天拿过 Product Hunt #1；官方 MCPMark 基准里对比 Supabase MCP 是 1.6 倍速度、省约 30% token（Claude Sonnet 4.5 下 47.6% vs 28.6% 的 Pass 率）。

```bash
# 云端版本（几步就完）：
npx @insforge/cli login
npx @insforge/cli create

# 自托管（Docker Compose）：
curl -fsSL https://raw.githubusercontent.com/InsForge/InsForge/main/deploy/setup.sh | sh -s ~/insforge
```

### 同类工具对比

| 工具 | 安装方式 | 核心特点 | 价格 | 热度 |
|------|---------|---------|------|------|
| **InsForge**（推荐）| `npx @insforge/cli create` | 原生按 Agent 交互设计，MCPMark 1.6× 速度、省 ~30% token | 免费层 500MB DB / Pro $25/月 | ⭐ 12.9K |
| Supabase + MCP | 官方 MCP 配置 | 生态最全，MCP 层是后期补的，Agent 上下文偏重 | 免费层 / Pro $25/月 | ⭐ 成熟项目 |
| Butterbase | `npx @butterbase/mcp` | 40+ MCP 工具覆盖最广（含 RAG、realtime） | 开源/托管 | ⭐ 3.2K |
| CloudBase AI Toolkit | MCP/Plugin 配置 | 腾讯云官方，国内接入快 | 厂商定价 | ⭐ 中等 |

  推荐理由就一条：它从零就是按 Agent 交互设计的，MCPMark 官方基准跑赢 Supabase MCP；而 Supabase 的 MCP 是后补的，Agent 要吃更多无用上下文。想要国内接入的话顺手用腾讯那套，但体验是另一路。

⭐ 12.9K · Apache-2.0 · TypeScript/Deno · [GitHub](https://github.com/InsForge/InsForge) · [官网](https://insforge.dev)

---

## Magnitude — 本地模型推理服务器，Agent 不再盲猜

![[magnitude-demo.png]]

  给 Agent 接本地模型的痛点在于选型：机器能跑什么模型、什么量化档位、上下文开多大，Ollama 把选择甩给你自己猜。Magnitude 先给硬件画像（芯片、内存、带宽），据此推荐模型和量化档并给出预估 tok/s，再自动下载、调优、运行，模型按需加载，空闲或内存紧张自动卸载。

  技术侧是 Rust 写的专用推理引擎，带投机解码（speculative decoding）和并发调优。对外暴露 OpenAI 兼容（`/inference/v1`）和 Anthropic 兼容（`/inference/anthropic`）双端点，跑在 `127.0.0.1:10100`——现有 agent（Claude Code、Codex、OpenClaw、Cline 等）不用改配置，安装时它直接写好 harness 配置。全离线，没有 API key，没有 token 计费。

  典型场景：`magnitude setup` 交互走一遍，Agent 接着就用本地模型干活；装了 CLI 之后再跑 `magnitude docs onboarding`，让 Agent 自己读文档完成配置。本周它刚爆发：单周涨了 1.9K stars 到 3.7K。

```bash
npm i -g @magnitudedev/cli
magnitude setup
```

### 同类工具对比

| 工具 | 安装方式 | 核心特点 | 价格 | 热度 |
|------|---------|---------|------|------|
| **Magnitude**（推荐）| `npm i -g @magnitudedev/cli` | 硬件画像自动推荐模型+调优，双协议端点，Agent 可自配置 | 开源免费 / Cloud $20/月 | ⭐ 3.7K（周增 +1.9K） |
| Ollama | brew/npm 安装 | 生态最大，模型选型全靠手 | 免费 | ⭐ 很高 |
| LM Studio | 桌面 App | 图形界面友好，非 CLI/Agent 优先 | 免费/Pro | ⭐ 中 |
| llama.cpp | 源码编译 | 底层能力最强，配置最原生态 | 免费 | ⭐ 很高 |

  选它的理由就是 README 那句话：你的 Agent 直接配 Ollama 等于在猜（Your agent would be guessing）——它把硬件画像、量化推荐、写 harness 配置串成一条引导流程，这是 Ollama 和 LM Studio 都没做的事。

⭐ 3.7K · Apache-2.0 · [GitHub](https://github.com/magnitudedev/magnitude)

---

## 中国专利 skill — 把研发的专利发明人一栏写上名字

![[patent-demo.jpg]]

  做了多年核心研发，专利发明人一栏从没写过自己的名字——这是多数研发的现实。这个技能包拆成 6 个子技能，覆盖专利全流程：交底书（发明/实用/外观分模板成文+查新）、申请文件（权利要求书/说明书/摘要/附图）、读专利（公开号或 PDF 转通俗笔记连入库 Obsidian 图库）、审查答复（案例分析+向量检索）、著录检索（按发明人/申请人/分类号查国知局）、政策简报（政策雷达）。

  比较能打的是技术部分：从产品图/结构图/CAD 提取线稿并标部件序号引出线（外观和实用新型附图的关键），mermaid 流程图直接渲染进 Word 交底书，国知局检索脚本落盘到 `outputs/patent-search/`。依赖是 Python 3.9+ 加 Playwright，浏览器就绪探测一条命令验证。

```bash
# Claude Code（Cursor 装到 ~/.cursor/skills/ 同理）：
git clone https://github.com/handsomestWei/patent-disclosure-skill.git .claude/skills/patent-disclosure-skill
pip install -r requirements.txt
```

### 同类工具对比

| 工具 | 安装方式 | 核心特点 | 价格 | 热度 |
|------|---------|---------|------|------|
| **中国专利 skill**（推荐）| git clone + pip install | 6 子技能全流程，附图线稿/国知局检索/Word 出稿 | 开源免费 | ⭐ 7.9K |
| 直接让通用 LLM 写交底 | 无 | 没查新没格式门禁，附图全手工 | 看模型 | — |
| 商业专利工具 | 订阅 | 面向代理机构，不面向研发个人 | 高 | — |

  这个品类几乎没有开源竞品：给研发个人的专利全流程技能包，我没搜到第二个（README 自己也是这么定位的）。

⭐ 7.9K · MIT · Python 3.9+ · [GitHub](https://github.com/handsomestWei/patent-disclosure-skill)
